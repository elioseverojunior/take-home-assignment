apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockerize.fullname" . }}
  labels:
    {{- include "dockerize.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dockerize.selectorLabels" . | nindent 4 }}
